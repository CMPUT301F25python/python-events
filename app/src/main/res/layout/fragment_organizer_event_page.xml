<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".organizer.OrganizerEventPageFragment">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <TextView
            android:id="@+id/event_name_label"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:layout_marginBottom="16dp"
            android:textColor="@android:color/black"
            android:textSize="24sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="Event Title" />

        <ImageView
            android:id="@+id/poster_image"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:layout_marginTop="10dp"
            android:contentDescription="Event Poster"
            android:scaleType="centerCrop"
            android:background="?android:attr/selectableItemBackground"
            android:clickable="true"
            android:focusable="true"
            app:layout_constraintTop_toBottomOf="@id/event_name_label"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintDimensionRatio="16:9"
            tools:src="@drawable/ic_image_placeholder" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/upload_poster_button"
            style="@style/Widget.Material3.Button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Upload Poster"
            app:layout_constraintTop_toBottomOf="@id/poster_image"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="10dp" />

        <Button
            android:id="@+id/request_qr_code"
            style="@style/PinkButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:text="@string/qr_code"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/upload_poster_button" />

        <LinearLayout
            android:id="@+id/organizer_button_container"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:orientation="vertical"
            android:visibility="gone"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/request_qr_code"
            tools:visibility="visible">

            <Button
                android:id="@+id/btnViewWaitingList"
                style="@style/OrganizerButton"
                android:text="@string/view_waiting_list" />

            <Button
                android:id="@+id/btnViewEntrantMap"
                style="@style/OrganizerButton"
                android:text="@string/view_entrant_map" />

            <Button
                android:id="@+id/btnAcceptedParticipants"
                style="@style/OrganizerButton"
                android:text="@string/accepted_participants" />

            <Button
                android:id="@+id/btnInvitedParticipants"
                style="@style/OrganizerButton"
                android:text="@string/invited_participants" />

            <Button
                android:id="@+id/btnCancelledParticipants"
                style="@style/OrganizerButton"
                android:text="@string/cancelled_participants" />

            <Button
                android:id="@+id/btnRunDraw"
                style="@style/OrganizerButton"
                android:text="@string/run_draw" />

            <Button
                android:id="@+id/btnFinalize"
                style="@style/FinalizeButton"
                android:text="@string/finalize" />

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</ScrollView>