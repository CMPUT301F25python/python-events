<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.RegistrationHistoryFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:orientation="vertical">

        <!-- Title for Container -->
        <TextView
            android:id="@+id/draw_results_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/draw_results"
            android:textAlignment="center"
            android:textColor="@color/primary_green"
            android:textSize="20sp"
            android:textStyle="bold" />

        <!-- Container for Draw Results -->
        <LinearLayout
            android:id="@+id/container_draw_results"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingTop="12dp" />

        <!-- TextView used for "no results" message -->
        <TextView
            android:id="@+id/no_history_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/no_draw_results"
            android:textSize="18sp"
            android:textColor="@color/grey"
            android:paddingTop="16dp" />

    </LinearLayout>

</ScrollView>
